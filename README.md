# 🤖 Telegram-бот на Python (aiogram)

## 📘 Описание проекта
Данный Telegram-бот создан в рамках курсовой работы.  
Он реализует клиент-серверную систему взаимодействия с пользователем и базой данных.  
Основная цель — автоматизация обработки запросов, регистрация пользователей и выполнение фоновых задач, таких как рассылки и резервное копирование данных.

Бот работает на библиотеке **aiogram**, поддерживает **FSM-состояния** (пошаговые диалоги), **локализацию (i18n)** и использует **PostgreSQL** для хранения информации о пользователях.

---

## ⚙️ Основные функции
- Регистрация и учёт пользователей по команде `/start`  
- Сохранение данных в базе PostgreSQL  
- Экспорт пользователей в CSV-файл  
- Планировщик (APScheduler) для ежедневных задач  
- FSM-диалоги для интерактивных опросов  
- Система логирования и резервного копирования  
- Мультиязычность (RU / EN)  

---

## 🧩 Используемые технологии
- **Python 3.10+**
- **aiogram** — взаимодействие с Telegram API  
- **asyncpg** — подключение к PostgreSQL  
- **APScheduler** — планировщик задач  
- **python-dotenv** — хранение конфигурации  
- **logging** — система журналирования  
- **pandas / csv** — экспорт данных  

---

## 🗂️ Структура проекта

project_root/
├─ run.py                # Точка входа, запуск бота и инициализация компонентов
├─ requirements.txt       # Список зависимостей проекта
├─ .env                   # Переменные окружения (токен, настройки БД)
├─ config.toml            # Основная конфигурация (расписание, пути, админы)
│
├─ /handlers/             # Логика обработки команд и сообщений
│   ├─ start.py           # /start — регистрация нового пользователя
│   ├─ help.py            # /help — помощь и справка по командам
│   ├─ admin.py           # /export, /report — функции администратора
│   └─ messages.py        # Обработка текстовых сообщений
│
├─ /models/               # Работа с базой данных
│   └─ database.py        # Подключение, запросы и сохранение данных
│
├─ /utils/                # Вспомогательные модули
│   ├─ config_loader.py   # Загрузка .env и config.toml
│   ├─ logger.py          # Логирование событий
│   └─ csv_export.py      # Экспорт данных пользователей в CSV
│
├─ /fsm/                  # FSM-сценарии (диалоги с пользователем)
│   └─ anon_question.py   # Пример диалога “Анонимный вопрос”
│
├─ /scheduler/            # Планировщик задач
│   └─ tasks.py           # Ежедневные отчёты и резервные копии
│
├─ /data/                 # Рабочие данные и результаты
│   ├─ users.csv          # Экспорт пользователей
│   ├─ backups/           # Папка для резервных копий
│   ├─ i18n/              # Локализация текстов (ru/en)
│   └─ templates/         # Шаблоны сообщений и отчётов
│
└─ /logs/                 # Логи работы бота
└─ bot.log


